import { Component } from '@angular/core';

@Component({
  moduleId: module.id,
  templateUrl: './contacts.page.html',
  host: { class: 'grid-row'}
})

export class ContactsPageComponent {
  constructor () { }
}
